# 平衡車除錯筆記

最常遇到的症狀：**車子傾到一定角度就往前衝，然後倒。** 這份是我們排這個問題的流程，原則就是一次只改一個東西，邊看 OLED（或 UART）邊往下走。

---

## 目前這版的參數對照

| 項目 | 設定 | 位置 |
|------|------|------|
| 角度來源 | `Angle_Balance = Pitch` | `control.c:171` Get_Angle |
| 角速度 | `Gyro_Balance = gyro[0]`（沒加負號） | `control.c:172` Get_Angle |
| 平衡式 | `Balance = -1500*(Middle - Angle) - 8.0*(0 - Gyro)` | `Balance()` |
| 「前進」對應 | 正 PWM（AIN1=1） | `Set_Pwm()` |
| 設定點 | `Middle_angle = 2.8` | `control.h:6` |
| 死區補償 | `MOTOR_DEADZONE = 250` | `control.h:9` + `Set_Pwm` |
| 平衡比例 | `Balance_Kp = 1500` | `control.c:53` |
| 平衡阻尼 | `Balance_Kd = 8.0` | `control.c:53` |
| 速度環 | `Velocity_Kp = -30, Velocity_Ki = -0.15` | `control.c:72` |
| PWM 上限 | ±3500（全速 7199 的一半左右） | `control.c:36-37` |

操作：
- 單擊按鍵（PA5）= 馬達開關（切 `Flag_Stop`）
- 這版沒 OLED、沒循跡，遙測走 UART 115200，印 `Angle / EncL / EncR`（`MiniBalance.c:65`）
- 傾角超過 ±40° 自動斷馬達（`Turn_Off`）

---

## 落地前一定要先驗的：速度環符號

速度環符號接反的話，車子會越推越快直接暴衝，很危險。先抬輪、開馬達、扶到接近直立，然後兩輪一起用手往前轉：
- 控制器想往後拉（反抗你）→ 符號對。
- 反而順著你越轉越快 → 把 `control.c:72` 改成 `Velocity_Kp = 30, Velocity_Ki = 0.15`（翻號）。

想單獨測純平衡（先不管速度環），就把 `control.c:72` 的 `Velocity_Kp / Velocity_Ki` 暫時改成 `0 / 0`，測完記得改回 `-30 / -0.15`。

---

## 步驟 0：安全

- 方向驗證（步驟 1~4）一律先把輪子抬離地，確定對了再落地（步驟 5）。
- 傾角超過 40° 會自動斷馬達，手動傾斜記得在 40° 內。
- 電量低表現會變差，先確認電池有電。

## 步驟 1：角度符號 + 設定點（馬達 OFF）

1. 開機後按一下關馬達，看 UART 印的 `Angle`。
2. 車身慢慢前傾，看 Angle 是往正還往負 → 記下「正角度是前傾還是後傾」。
3. 扶到車子真的會靜止的直立點，讀 Angle，這個數字就是 `Middle_angle`，差太多就改 `control.h:6`。

過關：知道角度正負方向，而且 `Middle_angle` 接近實際靜止角。

## 步驟 2：平衡驅動方向（輪子離地，馬達 ON）

1. 抬高輪子，按一下開。
2. 車身往前傾的時候，輪子應該往「會讓車前進」的方向轉（去接住車）。

對的話進步驟 3。如果轉反方向，代表平衡符號錯（只是僥倖站著），先把平衡符號修好，別往下做。

## 步驟 3：編碼器符號（輪子離地，馬達 OFF）

1. 關馬達，手轉左輪往前，看 EncL 是 + 還是 −；右輪看 EncR。
2. 記下車前進時左右編碼器各是什麼符號。

## 步驟 4：速度環方向（輪子離地，馬達 ON，速度環 -30/-0.15）

這步最容易出問題。

1. 抬高輪子、開馬達、扶在接近直立（平衡輸出大概是 0）。
2. 手把兩輪一起往前轉，控制器應該要反抗、想把輪子往後驅。

反抗（往後）就是對的，落地進步驟 5。如果順著往前加力，就是速度環符號錯，把 `Velocity_Kp/Ki` 翻號（改成 `+30 / +0.15`）。

「前衝倒」最常見的就是：步驟 2 對、步驟 4 錯，速度環在跟平衡搶。

## 步驟 5：落地調增益（1~4 都過了才做）

1. 先隔離：把 `control.c:72` 速度環暫時設 `0 / 0`（純平衡）落地測。
   - 大傾角還是前衝倒 → 是平衡的問題 → 加大 `Balance_Kd`（8 → 12 → 16）。
   - 純平衡 OK，只是推一下會滑走 → 是速度環 → 把 `-30/-0.15` 改回去，做第 2 點。
2. 速度環 P/I 分開調：先 `Velocity_Ki = 0`，只留 `Velocity_Kp = -30`，落地推一下：
   - 能減速停住 → Ki 從 ±0.05 慢慢加（大概抓 Ki ≈ Kp/200）。
   - 還是前衝 → 把 `Velocity_Kp` 幅度調小（-30 → -20），用最小的修正就好，別跟平衡搶。

---

## 速查：症狀 → 大概是哪裡

| 症狀 | 可能原因 |
|------|----------|
| 推一下滑走收不回（但沒倒） | 速度環太弱或關著 → 加大幅度 |
| 推一下加速逃跑、越來越快 | 速度環正回授（符號錯）→ 翻號 |
| 傾到角度才前衝倒 | 速度環在搶平衡（步驟 4），或平衡阻尼 Kd 不夠 |
| 站著自己往前飄最後倒 | `Middle_angle` 設定點偏了 |
| 接近直立時嗡嗡抖 | `MOTOR_DEADZONE` 太大，或 Kp 太大 |
