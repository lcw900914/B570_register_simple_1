# 平衡車除錯筆記

最常遇到的症狀：**車子傾到一定角度就往前衝，然後倒。** 這份是我們排這個問題的流程，原則就是一次只改一個東西，邊看 OLED 邊往下走。

---

## 目前這版的參數對照

| 項目 | 設定 | 位置 |
|------|------|------|
| 角度來源 | `Angle_Balance = Pitch` | `control.c` Get_Angle |
| 角速度 | `Gyro_Balance = -gyro[0]` | `control.c` Get_Angle |
| 平衡式 | `Balance = 500*(0.8 - Angle) - 2.5*Gyro` | `Balance()` |
| 「前進」對應 | 負 PWM | `Set_Pwm()` |
| 設定點 | `Middle_angle = 0.8` | `control.h:9` |
| 死區補償 | `MOTOR_DEADZONE = 350` | `control.h:17` |
| 平衡阻尼 | `Balance_Kd = 2.5`（偏小） | `Balance()` |
| 速度環 | `Velocity_Kp=-50, Velocity_Ki=-0.25` | `Velocity()` |

操作：
- 單擊按鍵 = 馬達開關（OLED 右下會顯示 `ON`/`OFF`）
- 不要雙擊，雙擊會進循跡模式 `LINE`
- OLED 的 `BAL` 畫面會顯示 Angle、EncoLEFT/RIGHT

一鍵除錯開關：`control.c` 最上面的 `//#define DEBUG_BALANCE_ONLY`
- 取消註解 → 速度環強制 0（純平衡），給步驟 5 隔離測試用
- 保持註解 → 正常（速度環 -50/-0.25）

---

## 步驟 0：安全

- 方向驗證（步驟 1~4）一律先把輪子抬離地，確定對了再落地（步驟 5）。
- 傾角超過 40° 會自動斷馬達，手動傾斜記得在 40° 內。
- 電量低表現會變差，先確認電池有電。

## 步驟 1：角度符號 + 設定點（馬達 OFF）

1. 開機先不要單擊（OLED 顯示 `OFF`）。
2. 車身慢慢前傾，看 Angle 是往正還往負 → 記下「正角度是前傾還是後傾」。
3. 扶到車子真的會靜止的直立點，讀 Angle，這個數字就是 `Middle_angle`，差太多就改 `control.h:9`。

過關：知道角度正負方向，而且 `Middle_angle` 接近實際靜止角。

## 步驟 2：平衡驅動方向（輪子離地，馬達 ON）

1. 抬高輪子，按一下開。
2. 車身往前傾的時候，輪子應該往「會讓車前進」的方向轉（去接住車）。

對的話進步驟 3。如果轉反方向，代表平衡符號錯（只是僥倖站著），先把平衡符號修好，別往下做。

## 步驟 3：編碼器符號（輪子離地，馬達 OFF）

1. 關馬達，手轉左輪往前，看 EncoLEFT 是 + 還是 −；右輪看 EncoRIGHT。
2. 記下車前進時左右編碼器各是什麼符號。

## 步驟 4：速度環方向（輪子離地，馬達 ON，速度環 -50/-0.25）

這步最容易出問題。

1. 抬高輪子、開馬達、扶在接近直立（平衡輸出大概是 0）。
2. 手把兩輪一起往前轉，控制器應該要反抗、想把輪子往後驅。

反抗（往後）就是對的，落地進步驟 5。如果順著往前加力，就是速度環符號錯，把 `Velocity_Kp/Ki` 翻號（改成 `+50 / +0.25`）。

「前衝倒」最常見的就是：步驟 2 對、步驟 4 錯，速度環在跟平衡搶。

## 步驟 5：落地調增益（1~4 都過了才做）

1. 先隔離：取消註解 `DEBUG_BALANCE_ONLY`（純平衡）落地測。
   - 大傾角還是前衝倒 → 是平衡的問題 → 加大 `Balance_Kd`（2.5 → 5 → 8）。
   - 純平衡 OK，只是推一下會滑走 → 是速度環 → 把 define 註解回去，做第 2 點。
2. 速度環 P/I 分開調：先 `Velocity_Ki = 0`，只留 `Velocity_Kp = -50`，落地推一下：
   - 能減速停住 → Ki 從 ±0.05 慢慢加。
   - 還是前衝 → 把 `Velocity_Kp` 幅度調小（-50 → -20），用最小的修正就好，別跟平衡搶。

---

## 速查：症狀 → 大概是哪裡

| 症狀 | 可能原因 |
|------|----------|
| 推一下滑走收不回（但沒倒） | 速度環太弱或關著 → 加大幅度 |
| 推一下加速逃跑、越來越快 | 速度環正回授（符號錯）→ 翻號 |
| 傾到角度才前衝倒 | 速度環在搶平衡（步驟 4），或平衡阻尼 Kd 不夠 |
| 站著自己往前飄最後倒 | `Middle_angle` 設定點偏了 |
| 接近直立時嗡嗡抖 | `MOTOR_DEADZONE` 太大，或 Kp 太大 |
